#!/bin/bash

BIN=$1
OUTDIR=$2
TIMEOUT=$3
INDIR=$4

timeout -k 9 ${TIMEOUT}s $BIN 'for this du the key thing is to have a ccl at exactly the right length more [@@]' 'sub doesnt matter' < $INDIR/moni/rr1.t &> $OUTDIR/o3985

