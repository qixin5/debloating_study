#!/bin/bash

BIN=$1
OUTDIR=$2
TIMEOUT=$3
INDIR=$4
CURRDIR=$(pwd)

timeout -k 9 ${TIMEOUT}s $BIN error -dl -Cam $INDIR/in.0  > $OUTDIR/o180 2>&1

cat lex.yy.c >> $OUTDIR/o180
cat error >> $OUTDIR/o180
