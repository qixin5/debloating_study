#!/bin/bash

BIN=$1
OUTDIR=$2
TIMEOUT=$3
INDIR=$4
CURRDIR=$(pwd)

timeout -k 9 ${TIMEOUT}s $BIN error -d -CF $INDIR/in.closure  > $OUTDIR/o66 2>&1

cat lex.yy.c >> $OUTDIR/o66
cat error >> $OUTDIR/o66
