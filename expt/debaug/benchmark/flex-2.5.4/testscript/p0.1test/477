#!/bin/bash

BIN=$1
OUTDIR=$2
TIMEOUT=$3
INDIR=$4
CURRDIR=$(pwd)

timeout -k 9 ${TIMEOUT}s $BIN error -l -Crae $INDIR/in.ch  > $OUTDIR/o477 2>&1

cat lex.yy.c >> $OUTDIR/o477
cat error >> $OUTDIR/o477
