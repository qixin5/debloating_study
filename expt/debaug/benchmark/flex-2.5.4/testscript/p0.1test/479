#!/bin/bash

BIN=$1
OUTDIR=$2
TIMEOUT=$3
INDIR=$4
CURRDIR=$(pwd)

timeout -k 9 ${TIMEOUT}s $BIN error -dF -Cae $INDIR/in.ch  > $OUTDIR/o479 2>&1

cat lex.yy.c >> $OUTDIR/o479
cat error >> $OUTDIR/o479
