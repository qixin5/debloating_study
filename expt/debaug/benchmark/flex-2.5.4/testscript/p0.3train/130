#!/bin/bash

BIN=$1
OUTDIR=$2
TIMEOUT=$3
INDIR=$4
CURRDIR=$(pwd)

timeout -k 9 ${TIMEOUT}s $BIN error -dl -Craem $INDIR/in.exp  > $OUTDIR/o130 2>&1

cat lex.yy.c >> $OUTDIR/o130
cat error >> $OUTDIR/o130
