#!/bin/bash

BIN=$1
OUTDIR=$2
TIMEOUT=$3
INDIR=$4
CURRDIR=$(pwd)

timeout -k 9 ${TIMEOUT}s $BIN error -p -C $INDIR/in.ch_exp_5  > $OUTDIR/o160 2>&1

cat lex.yy.c >> $OUTDIR/o160
cat error >> $OUTDIR/o160
