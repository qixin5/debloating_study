#!/bin/bash

BIN=$1
OUTDIR=$2
TIMEOUT=$3
INDIR=$4
CURRDIR=$(pwd)

timeout -k 9 ${TIMEOUT}s $BIN error -dF -Cae $INDIR/in.0  > $OUTDIR/o461 2>&1

cat lex.yy.c >> $OUTDIR/o461
cat error >> $OUTDIR/o461
