#!/bin/bash

BIN=$1
OUTDIR=$2
TIMEOUT=$3
INDIR=$4
CURRDIR=$(pwd)

timeout -k 9 ${TIMEOUT}s $BIN error -dl -Crae $INDIR/in.exp  > $OUTDIR/o452 2>&1

cat lex.yy.c >> $OUTDIR/o452
cat error >> $OUTDIR/o452
